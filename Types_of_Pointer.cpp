// Dangling pointer - Dangling pointer is used to point a memory location that has been realocate.

// Wild pointer - Wild pointer is used to point pointer that has not been initialised a valid memory address.

// Null pointer - We use null pointer when we do not have an address to asign a pointer , it is like a GPS device that has not been setup with a destination.

// Memory leak - 